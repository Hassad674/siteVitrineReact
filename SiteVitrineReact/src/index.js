import React from "react";
import ReactDOM from "react-dom";
import App from "./App";
import "./style/index.scss";

// Rendu du composant racine App dans l'élément avec l'ID "root" du DOM
ReactDOM.render(<App />, document.getElementById("root"));
